# 레포지토리 문서·폴더 관리 규칙

- 제정일: 2026-09-02 (KST)
- 적용 범위: `01_project_planning`부터 `08_archives`까지의 카테고리 폴더와 이후 추가되는 공식 카테고리
- 연계 문서: `문서_버전관리대장.md`

## 1. 관리 원칙

1. 카테고리 폴더의 **최상위 경로에는 문서 계열별 최신 버전만 둔다.**
2. 최신 버전이 아닌 파일은 같은 카테고리의 `archive/` 하위 폴더로 이동한다.
3. 같은 문서 계열의 최신본은 원칙적으로 1개만 유지한다.
4. 새 버전을 만들 때 이전 파일을 덮어쓰거나 삭제하지 않는다.
5. 최신본 변경과 동시에 `문서_버전관리대장.md`의 최신본 경로와 변경 이력을 갱신한다.
6. `upload/`, `tmp/` 및 각종 임시 작업 폴더는 정리 전 대기 공간이며 공식 버전의 위치로 사용하지 않는다.

여기서 **최상위**란 카테고리 폴더 바로 아래를 뜻한다. 파일 탐색기의 화면 정렬 순서와는 관계없다.

## 2. 표준 폴더 구조

```text
카테고리/
├── 문서명_YYYYMMDD_v03_현행본.docx       # 최신본
├── 분석결과_YYYYMMDD_v02_현행본.xlsx     # 최신본
└── archive/
    ├── 문서명/
    │   ├── 문서명_YYYYMMDD_v01_초안.docx
    │   └── 문서명_YYYYMMDD_v02_중간본.docx
    └── 분석결과/
        └── 분석결과_YYYYMMDD_v01_초안.xlsx
```

- 카테고리 루트: 현재 사용하는 최신본
- `archive/`: 더 이상 최신이 아닌 이전 버전
- `archive/문서계열/`: 동일 계열의 이전 버전을 묶는 선택적 하위 폴더

이전 버전이 1~2개뿐이라도 원칙적으로 `archive/`에 보관한다. 이전 버전이 많거나 문서 계열이 여러 개면 `archive/` 아래에 문서 계열별 폴더를 추가한다.

## 3. 카테고리 기준

| 폴더 | 관리 대상 |
|---|---|
| `01_project_planning/` | 프로젝트 계획서, 기획서, 일정 및 역할 문서 |
| `02_research/` | 선행연구, 시장·서비스 조사, 비교자료 |
| `03_data_modeling/` | ERD, 스키마, 데이터 사전, 공식 데이터 스냅샷 |
| `04_analysis_design/` | 가설, 지표 정의, 분석 설계, EDA 및 분석 결과 |
| `05_presentations/` | 발표자료, 발표 대본, 시각화 산출물 |
| `06_guides_and_prompts/` | 실행 가이드, 프롬프트, 작업 절차 |
| `07_training_materials/` | 일차별 실습 문제, 과제, 제출본 |
| `08_archives/` | 프로젝트 전체에서 사용이 종료된 묶음 또는 카테고리 미지정 보존자료 |

각 카테고리의 `archive/`는 **동일 문서의 이전 버전**을 보관한다. 루트의 `08_archives/`는 **사용이 종료된 문서 묶음이나 전체 보존자료**를 관리하므로 용도가 다르다.

## 4. 파일명 규칙

기본 형식은 다음과 같다.

```text
문서계열_주제_YYYYMMDD_vNN_상태_추가정보.확장자
```

예시:

```text
호텔검색_가설문서_20260902_v06_현행본.docx
호텔검색_가설문서_20260902_v05_중간본.docx
호텔검색_분석결과_20260903_v02_현행본.xlsx
```

### 작성 기준

- 날짜는 `YYYYMMDD` 8자리로 쓴다.
- 버전은 `v01`, `v02`, `v03`처럼 두 자리 이상으로 쓴다.
- 내용이 변경되면 버전을 올린다. 단순 이동만으로는 버전을 올리지 않는다.
- 최신 파일에는 가능하면 `현행본`, 제출이 완료된 파일에는 `제출본`을 표시한다.
- `최종`, `진짜최종`, `최종수정`, `final2`, `복사본` 같은 표현은 사용하지 않는다.
- 작성자 정보가 꼭 필요하지 않으면 파일명에 개인 이름을 넣지 않는다.
- 개인정보가 포함된 원본과 마스킹본이 함께 있다면 공개·공유 기준 파일은 `작성자마스킹` 또는 `비식별`로 표시한다.

## 5. 최신 버전 판정 기준

다음 순서로 최신본을 판단한다.

1. `문서_버전관리대장.md`에서 해당 문서 계열의 현재 기준 파일을 확인한다.
2. 파일명의 버전 번호를 비교한다.
3. 버전이 같으면 상태가 `현행본` 또는 `제출본`인 파일을 우선한다.
4. 버전과 상태가 모두 같으면 문서 내용과 변경 이력을 확인한다.
5. 판단이 어려우면 임의로 삭제하거나 이동하지 않고 `최신본 확인 필요`로 기록한다.

파일의 수정 시각만으로 최신본을 결정하지 않는다. 다운로드, 복사 또는 이동 과정에서 수정 시각이 바뀔 수 있기 때문이다.

## 6. 새 버전 반영 절차

1. 새 파일의 문서 계열, 작성일, 버전과 상태를 확인한다.
2. 카테고리 루트에 있는 기존 최신본을 찾는다.
3. 새 파일의 버전이 실제로 더 높은지 확인한다.
4. 기존 최신본을 해당 카테고리의 `archive/문서계열/`로 이동한다.
5. 새 최신본을 카테고리 루트에 배치한다.
6. 루트에 같은 문서 계열의 최신 후보가 둘 이상 남아 있지 않은지 확인한다.
7. `문서_버전관리대장.md`의 최신본 경로, 버전, 상태와 변경 이력을 갱신한다.
8. 이메일, 이름, 전화번호 등 개인정보와 비밀정보가 포함되지 않았는지 확인한다.
9. `git status`와 `git diff --stat`으로 이동·추가 범위를 확인한다.
10. 관련된 파일 이동과 관리대장 변경을 하나의 커밋으로 기록한다.

## 7. 문서 계열 판단 방법

아래 항목이 같으면 같은 문서 계열로 본다.

- 문서의 목적과 주요 제목
- 담당 과제 또는 분석 주제
- 버전 번호를 제외한 기본 파일명
- 이전 문서를 수정·보완해 만든 관계

다음은 별도 문서 계열로 관리할 수 있다.

- 원본 양식과 작성 완료본
- 내부 작업본과 외부 제출본
- 실제 데이터와 비식별·합성 데이터
- 분석 보고서와 발표자료

별도 계열로 나눌 때는 `문서_버전관리대장.md`에 두 계열의 관계를 기록한다.

## 8. 데이터 파일 관리

- 카테고리 루트에는 현재 분석에 사용하는 공식 데이터만 둔다.
- 이전 데이터 스냅샷은 `03_data_modeling/archive/datasets/`에 보관한다.
- 스냅샷 보관 폴더는 `YYYY-MM-DD_vNN` 형식으로 만들고, 같은 버전의 DB·CSV·테이블 명세를 함께 보관한다.
- 데이터 파일명에는 기준일과 버전을 반드시 포함한다.
- SQLite, CSV, XLSX 사이에 같은 데이터를 저장했다면 각각의 용도를 관리대장에 기록한다.
- DB와 여기서 추출한 CSV에는 같은 기준일과 버전을 사용한다.
- 최신 데이터와 이전 데이터의 파일명, 행 수, SHA-256, 검증 결과는 `03_data_modeling/데이터_버전관리대장.md`에 기록한다.
- 원본 데이터는 직접 덮어쓰지 않는다. 정제·마스킹·증강 결과는 새 버전으로 저장한다.
- 개인정보가 확인되지 않은 데이터는 Git에 추가하지 않는다.
- 대용량 파일은 원격 저장소 제한과 Git LFS 적용 여부를 먼저 확인한다.

## 9. 예외 처리

- 외부에서 받은 원본 자료는 파일명 변경이 출처 추적을 방해할 경우 원래 이름을 유지할 수 있다.
- 강사 제공 양식과 문제지는 작성본과 별도 계열로 보존한다.
- 코드에서 고정 경로로 참조하는 파일을 이동할 때는 코드·노트북·문서의 참조 경로를 함께 수정한다.
- 동일한 내용의 중복 파일은 해시값을 확인한 뒤 하나만 유지할 수 있다. 삭제 여부가 불분명하면 `archive/duplicates_review/`로 이동한다.
- 현재 작업 중인 파일은 공식 최신본과 구분해 로컬 임시 폴더에서 관리하고, 검토가 끝난 뒤 카테고리로 승격한다.

## 10. 금지 사항

- 카테고리 루트에 같은 문서 계열의 여러 버전을 함께 두지 않는다.
- 버전 번호가 없는 작업본을 최신본으로 지정하지 않는다.
- 새 버전으로 이전 버전을 덮어쓰지 않는다.
- 관리대장 갱신 없이 최신본 경로를 바꾸지 않는다.
- `upload/`와 `tmp/`의 파일을 검토 없이 Git에 추가하지 않는다.
- 개인정보, 인증정보, API 키 또는 비밀정보가 포함된 파일을 커밋하지 않는다.
- 다른 사용자의 변경 파일을 정리 대상이라는 이유만으로 삭제하지 않는다.

## 11. Git 커밋 권장 방식

폴더 정리와 최신본 승격은 내용 수정과 구분해 커밋한다.

```text
docs: 호텔 검색 가설문서 v06을 현행본으로 승격
docs: 이전 기획서 버전을 archive로 이동
data: 2026-09-03 비식별 데이터 v02 반영
chore: 문서 버전관리대장 경로 갱신
```

하나의 커밋에는 가능한 한 하나의 문서 계열 또는 하나의 정리 목적만 포함한다.

## 12. 최종 점검표

- [ ] 카테고리 루트에는 문서 계열별 최신본만 있는가?
- [ ] 이전 버전이 `archive/` 아래에 있는가?
- [ ] 최신본의 날짜·버전·상태가 파일명에 표시됐는가?
- [ ] 이전 버전을 삭제하거나 덮어쓰지 않았는가?
- [ ] `문서_버전관리대장.md`의 최신본 경로를 갱신했는가?
- [ ] 코드와 노트북의 파일 참조 경로가 유효한가?
- [ ] 개인정보와 비밀정보를 점검했는가?
- [ ] `upload/`, `tmp/` 및 임시 산출물이 스테이징되지 않았는가?
- [ ] 커밋 대상에 관련 없는 사용자 변경이 포함되지 않았는가?

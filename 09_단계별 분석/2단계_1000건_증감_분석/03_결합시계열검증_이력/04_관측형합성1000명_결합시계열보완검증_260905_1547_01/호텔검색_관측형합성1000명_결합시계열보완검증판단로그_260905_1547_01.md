# STEP V1 결합·시계열 보완검증 판단 로그

- 최종 판정: **STEP V1=CONDITIONAL PASS**
- 입력 불변: **PASS**
- 기존 분석 회귀: **PASS**

## 세부 판정

- 고차 조건부 결합확률: **PASS** — max major diff=0.440678pp; log-loss delta=-0.049403; leakage folds=0. joint/conditional preservation diagnostic only; non-causal
- 시계열 역전: **PASS** — reversal failures=0; logical-order failures=0. canonical timestamp order and explicit event/search precedence checks
- 원본 경험분포 보존: **PASS** — median 12.0/12.0; max 249.0/249.0. empirical repetition, not new distributional generation
- 로그정규 적합성: **WARN** — p original=0.019960; p S0=0.001996. fitted-parameter KS is auxiliary; do not modify time values to force fit
- 기존 분석 회귀: **PASS** — STEP2 mismatch=0; traces=0; integrity=0; old STEP B=0. no existing hypothesis result changed

## 권위 해석

- 고차 결합 결과는 관측된 결합분포와 조건부 의존구조의 보존 진단이며 인과효과가 아니다.
- 시간 간격은 원본 경험분포를 반복 보존했지만 로그정규 적합은 5% 기준에서 지지되지 않았다.
- S0는 로그정규분포에서 시간을 새로 생성한 데이터가 아니다.
- canonical EVENT 정렬에는 역전이 없지만 독립 원천 순번이 없어 수집 순서 자체는 NOT_TESTABLE이다.
